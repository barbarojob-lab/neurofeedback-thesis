# ml-service/requirements.txt
#
# Dependencias del servicio Python de análisis EEG.
# Instalar con: pip install -r requirements.txt
#
# Versiones mínimas probadas (Python 3.10+):
#   numpy   >= 1.24  — operaciones matriciales (FFT, PLV, features)
#   scipy   >= 1.11  — coherence(), hilbert(), butter(), filtfilt()
#   scikit  >= 1.4   — SVM, Random Forest, Pipeline, CV, métricas
#   fastapi >= 0.110 — servidor REST + WebSocket
#   uvicorn >= 0.27  — ASGI server para FastAPI
#   pydantic >= 2.0  — validación de requests (integrado con FastAPI)
#   joblib  >= 1.3   — serialización del modelo entrenado

numpy>=1.24
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3
mne>=1.6
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
websockets>=12.0
